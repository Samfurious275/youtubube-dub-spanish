# CPU-only torch. Without this index pip pulls the CUDA build on Linux, which is
# ~2.5 GB of GPU libraries a free CPU Space can never use.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
torchaudio==2.2.2

numpy<2
demucs==4.0.1
soundfile
pillow

# speech -> text
faster-whisper>=1.0.3
ctranslate2>=4.0,<4.5
onnxruntime

# english -> spanish (offline, no API key)
argostranslate>=1.9.6

# spanish text -> speech (free Edge voices, no API key)
edge-tts>=6.1.9

gradio>=4.44
